import React, {useContext, createContext} from 'react';
const search = () => {
  const input = document.getElementbyId("user-search");
}
const result = cards.filter(checkCard);
function checkCard (name) {
  return item.startsWith (name);
}
  const filtered.Cards = {result.innerHTML};
app.post("/search",(req,res)=>{
        if (search.onclick = true)
          then(function( input.push({"input":input}) ); 
              return result (res.send(filtered.Cards))
        
        )}
